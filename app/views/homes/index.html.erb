
<script type="text/javascript">
  var element = document.getElementById("home-link");

</script>

<!-- Header -->
<header id="top" class="header">
    <div class="text-vertical-center">
      <img src="/img/logoWhite.png" width="40%" alt="로고" style="margin-bottom: 30px">
        <br>
        <%= link_to 'Write Down What you :D:D Today 😎', new_home_path, :class => "btn btn-home btn-lg" %>
    </div>
</header>


<p id="notice"><%= notice %></p>

<h1>Listing Homes</h1>

<section>
  <table>
    <thead>
      <tr>
        <th>Content</th>
        <th>Tag</th>
        <th>Writedate</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @homes.each do |home| %>
        <tr>
          <td><%= home.content %></td>
          <td><%= home.tag %></td>
          <td><%= home.writeDate %></td>
          <td><%= link_to 'Show', home %></td>
          <td><%= link_to 'Edit', edit_home_path(home) %></td>
          <td><%= link_to 'Destroy', home, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Home', new_home_path %>
</section>
